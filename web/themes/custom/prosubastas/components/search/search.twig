{#
/**
 * @file
 * Template for Buscador component.
 *
 * Available config:
 * - title: Page title.
 * - filters: Rendered exposed filter form from Views (marca, combustible, ciudad).
 * - results: Rendered rows from the View (each row is a subasta node).
 * - pager: Rendered pager from Views.
 * - exposed_form: The full exposed filter form rendered by Drupal Views.
 *
 * Each result row exposes (via Views fields with relationship field_vehiculo):
 * - title, image (field_galeria), marca, modelo, version
 * - kilometraje, combustible, ciudad
 * - price_starting, price_buy_now, currency_code, workflow
 */
#}
